# Next Session Plan

Recommended order after usage refreshes:

1. Keep the sigil and trait data audited against current GBFRDataTools IDs.
2. Recheck `skill_status.tbl` if a future game patch changes trait level data.
3. Keep secondary compatibility broad unless strict in-game legality is requested again.
4. Improve the GUI search/filter experience.
5. Add queue edit/duplicate/move controls if needed.
6. Package a clean release folder and test it outside the source tree.
7. Test only with copied save files.

## Do Not Do Yet

- Do not auto-fill unknown trait max levels.
- Do not overwrite the original save.
- Do not add runtime memory editing, game executable patches, online behavior, anti-cheat bypasses, DRM bypasses, or server validation bypasses.
